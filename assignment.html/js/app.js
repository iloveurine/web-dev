// Minimal common script for student-level site
function initCommon() {
  // Intentionally minimal: no theme toggle or complex behavior.
}

document.addEventListener('DOMContentLoaded', initCommon);
